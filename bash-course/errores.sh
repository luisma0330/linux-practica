#! bin/bash

set -e # En caso de error el script se detiene inmediatamente.

set -x # Muestra la ejecucion de cada linea ejecutada por pantalla.

set -u # Cuando uno de los comandos usa una variable que no esta declarada lo maneja como un error y detiene la ejecucion del programa.